from GUI import baseStationApplication as application
import sys
import serial
import terminal

if __name__ == "__main__":
    
    app = application(sys.argv)
    #main = terminal()
    #main.show()
    #sys.exit(app.exec_())